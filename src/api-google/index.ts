/**
 * Exports all the functions from the auth and driveService files
 * - this way we can import all the functions from this file by dir name only (without having to know the file names)
 * @example `import { getRootFolderId } from '../api-google'
 */

export * from './auth';
export * from './driveService';
